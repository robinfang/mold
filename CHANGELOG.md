# Changelog

## 0.3.0 - 2026-06-12

- Add template inspection API:
  - `Template::inspect()`
  - `Engine::inspect(...)`
  - top-level `inspect(...)`
  - `TemplateInspection` with `variables`, `filters`, and `includes`
- Add inspection documentation.
- Add include/loader recipe.
- Sync delivery docs and capability comparison with current implementation.

## 0.2.0

- Add length filter, loop metadata, WASM export, and expanded docs/examples.
