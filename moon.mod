name = "robinfang/mold"

version = "0.1.0"

readme = "README.md"

repository = "git@code.gitlink.org.cn:robinfang/mold.git"

license = "Apache-2.0"

keywords = [ "moonbit", "template", "templating" ]

description = "A lightweight template engine for MoonBit."

options(
  source: "src",
)
